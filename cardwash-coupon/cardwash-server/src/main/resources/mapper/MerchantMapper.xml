<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moca.cardwash.mapper.MerchantMapper">

    <resultMap id="BaseResultMap" type="com.moca.cardwash.entity.Merchant">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, name, status, create_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <select id="selectAllByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO merchant (user_id, name, status)
        VALUES (#{userId}, #{name}, #{status})
    </insert>

    <update id="update">
        UPDATE merchant
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
