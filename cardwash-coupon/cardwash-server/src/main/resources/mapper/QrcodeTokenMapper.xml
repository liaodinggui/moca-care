<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moca.cardwash.mapper.QrcodeTokenMapper">

    <resultMap id="BaseResultMap" type="com.moca.cardwash.entity.QrcodeToken">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="token" property="token"/>
        <result column="expire_time" property="expireTime"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, token, expire_time, status, create_time
    </sql>

    <select id="selectByToken" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM qrcode_token
        WHERE token = #{token}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO qrcode_token (order_id, token, expire_time, status)
        VALUES (#{orderId}, #{token}, #{expireTime}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE qrcode_token
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
